@model WebLab.Группы

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Группы</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Название" class="control-label"></label>
                <input asp-for="Название" class="form-control" />
                <span asp-validation-for="Название" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ВузId" class="control-label"></label>
                <select asp-for="ВузId" class ="form-control" asp-items="ViewBag.ВузId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="text-decoration-none" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
