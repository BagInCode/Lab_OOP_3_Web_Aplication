@model WebLab.Сценарии

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Сценарии</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="КВоАктёров" class="control-label"></label>
                <input asp-for="КВоАктёров" class="form-control" />
                <span asp-validation-for="КВоАктёров" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Описание" class="control-label"></label>
                <input asp-for="Описание" class="form-control" />
                <span asp-validation-for="Описание" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
